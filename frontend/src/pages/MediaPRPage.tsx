import React from 'react';
import { Container, Typography } from '@mui/material';

const MediaPRPage = () => {
  return (
    <Container sx={{ py: 4 }}>
      <Typography variant="h4">Media & Press Releases</Typography>
      <Typography>Latest news and updates about skating events.</Typography>
    </Container>
  );
};

export default MediaPRPage;